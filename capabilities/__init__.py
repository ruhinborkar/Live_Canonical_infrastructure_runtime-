"""Generic, reusable operational capability layer.

Each capability is a self-contained module with a clean interface that can
attach to the background runtime engine without modifying core architecture.
Capabilities are domain-agnostic: no organisation-specific or classified logic.
"""
